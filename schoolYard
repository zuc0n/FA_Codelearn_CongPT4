//vì gạch, sân có hình chữ nhật nên số gạch cần lát sẽ bằng chiều dài/kích thước gạch nhân vs chiều rộng/ kích thước gạch.

function schoolYard(n, m, a) {
    return Math.ceil(n / a) * Math.ceil(m / a);
}
